# U-005 — Bind 1 to primary and 2 to pistol

Epic: **Weapon feel**. Status, priority and dependencies live only in [BACKLOG.md](../../BACKLOG.md).
Source: [owner feedback, 2026-09-26](2026-09-26-feedback.md), plus any legacy bug named below.

## Problem / outcome

The owner wants direct slot selection: 1 = primary, 2 = pistol.

## Scope

Route the keys by loadout slot rather than incidental weapon-array order. Update player-facing help/onboarding. U-022 will extend primary selection for the two-primary support character.

## Start here

These are entry points, not a prescribed edit list; verify current code before changing it.

- `packages/client/src/input/LocalInput.ts`
- `packages/client/src/main.ts`
- `packages/client/src/ui/WeaponPanel.ts`
- `packages/client/src/ui/RoomLobby.ts`

## Acceptance criteria

- [ ] 1 selects the equipped primary and 2 selects the equipped pistol; repeated presses are idempotent for ordinary loadouts.
- [ ] Absent slots are a safe no-op with appropriate UI feedback; picking up a primary does not change the pistol binding.
- [ ] While the Q command wheel is open, its 1–6 squad selection retains priority; typing in menus/text fields does not switch guns.
- [ ] Switching respects downed/dead state, reload cancellation rules and server authority; help matches behavior.

## Verification

Add input routing/weapon-selection regressions, including Q+number and menu focus. Check local and hosted selection.

Follow the shared [delivery workflow](../../.claude/commands/next-task.md), including `pnpm verify`. Record commands/results and observed acceptance evidence below. Visual/audio quality and owner decisions are not proven by a passing test.

## Handoff / evidence

Not started. No reproduction, implementation, playtest or owner approval is claimed.
