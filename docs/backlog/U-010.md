# U-010 — Let enemies interrupt uploads by using the lever

Epic: **Mission**. Status, priority and dependencies live only in [BACKLOG.md](../../BACKLOG.md).
Source: [owner feedback, 2026-09-26](2026-09-26-feedback.md), plus any legacy bug named below.

## Problem / outcome

Protecting an upload should mean defending the equipment from enemy action, with freedom to position around it.

## Scope

Give configured enemies a reachable lever-use objective that interrupts the upload through the same authoritative interaction rules. Retain combat behavior and allow the squad to prevent the use.

## Start here

These are entry points, not a prescribed edit list; verify current code before changing it.

- `packages/server/src/ai`
- `packages/server/src/session/Session.ts`
- `packages/server/src/session/mission.ts`
- `packages/shared/src/data`

## Acceptance criteria

- [ ] An assigned enemy navigates to the lever and performs a visible, interruptible use action; merely entering an area does not sabotage.
- [ ] Killing/downing/displacing the user before completion prevents sabotage; unreachable paths release reservations and recover.
- [ ] A successful use interrupts the upload exactly once, replicated to every client; an ally can resume via U-009.
- [ ] Enemy assignments and lever state recover on checkpoint retry without duplicate users or deadlocks.

## Verification

Add AI interaction and session integration scenarios for successful use, interrupted use, blocked path and retry; record a solo-with-bots demonstration.

Follow the shared [delivery workflow](../../.claude/commands/next-task.md), including `pnpm verify`. Record commands/results and observed acceptance evidence below. Visual/audio quality and owner decisions are not proven by a passing test.

## Handoff / evidence

Not started. No reproduction, implementation, playtest or owner approval is claimed.
