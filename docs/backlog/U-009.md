# U-009 — Add an interaction-driven upload objective

Epic: **Mission**. Status, priority and dependencies live only in [BACKLOG.md](../../BACKLOG.md).
Source: [owner feedback, 2026-09-26](2026-09-26-feedback.md), plus any legacy bug named below.

## Problem / outcome

The owner rejects standing inside a box for a timer. They want an interaction, such as protecting a file upload from enemies using a lever.

## Scope

Add one reusable objective: a living ally interacts with a terminal to start an upload; the upload continues without allies occupying an area. Scope this task to the objective state machine, validated interaction path, checkpoint data and HUD. U-010 adds hostile lever behavior; U-011 migrates mission content. Proposed reversible default: enemy lever use pauses progress; an ally restarts at the terminal without losing accumulated progress.

## Start here

These are entry points, not a prescribed edit list; verify current code before changing it.

- `packages/shared/src/sim/mission.ts`
- `packages/server/src/session/mission.ts`
- `packages/server/src/session/Session.ts`
- `packages/client/src/ui/missionHud.ts`
- `packages/shared/src/net/schema.ts`

## Acceptance criteria

- [ ] The server validates actor state, reach/line-of-sight and objective phase before accepting interaction; stale/duplicate/out-of-range requests cannot advance it.
- [ ] The upload advances with no ally in a capture box; the HUD explains start, active, interrupted and complete states.
- [ ] Interruption/resume behavior is explicit and data-driven; progression is bounded and cannot complete twice.
- [ ] Checkpoint/reconnect restores the correct objective, progress and interactable state for all clients.

## Verification

Unit-test transitions and invalid interactions; add headless multiplayer/checkpoint coverage. Revise wire protocol version if its contract changes. Keep legacy objective types compatible.

Follow the shared [delivery workflow](../../.claude/commands/next-task.md), including `pnpm verify`. Record commands/results and observed acceptance evidence below. Visual/audio quality and owner decisions are not proven by a passing test.

## Handoff / evidence

Not started. No reproduction, implementation, playtest or owner approval is claimed.
