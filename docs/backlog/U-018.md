# U-018 — Equip dropped guns into the primary slot

Epic: **Loot**. Status, priority and dependencies live only in [BACKLOG.md](../../BACKLOG.md).
Source: [owner feedback, 2026-09-26](2026-09-26-feedback.md), plus any legacy bug named below.

## Problem / outcome

Enemy guns should be usable as the player’s primary, not a cosmetic pickup or a pistol replacement.

## Scope

Implement a server-authoritative pickup interaction that atomically replaces the selected primary, preserves the pistol and handles ammo/reload state. Reversible default: the replaced primary becomes one world pickup with its current ammo, using U-017’s lifecycle.

## Start here

These are entry points, not a prescribed edit list; verify current code before changing it.

- `packages/server/src/session/Session.ts`
- `packages/shared/src/sim/classes.ts`
- `packages/client/src/main.ts`
- `packages/client/src/ui/WeaponPanel.ts`
- `packages/shared/src/net/schema.ts`

## Acceptance criteria

- [ ] Only an alive eligible soldier in valid range/line-of-sight can pick up; stale IDs and client-supplied ammo are rejected.
- [ ] Two players racing for a gun produce one winner, one inventory transfer and no duplication.
- [ ] HUD/model/firing/reload/audio switch to the picked-up weapon; 1 selects that primary and 2 retains the pistol.
- [ ] Replacement drops, disconnect, respawn and checkpoint policies are explicit and tested; U-022 later handles which of two support primaries is replaced.

## Verification

Add multiplayer race and state-transfer integration coverage; test pickup→switch→reload→death/retry end to end.

Follow the shared [delivery workflow](../../.claude/commands/next-task.md), including `pnpm verify`. Record commands/results and observed acceptance evidence below. Visual/audio quality and owner decisions are not proven by a passing test.

## Handoff / evidence

Not started. No reproduction, implementation, playtest or owner approval is claimed.
