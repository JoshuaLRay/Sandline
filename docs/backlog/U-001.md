# U-001 — Restore reliable mission enemy pressure

Epic: **Mission**. Status, priority and dependencies live only in [BACKLOG.md](../../BACKLOG.md).
Source: [owner feedback, 2026-09-26](2026-09-26-feedback.md), plus any legacy bug named below.

## Problem / outcome

After the initial spawn, mission-01 can become empty while an objective timer keeps running. Reported after checkpoint retry or killing almost a whole squad; a hidden, unaware survivor may remain. The cause is unconfirmed.

## Scope

Reproduce and fix the encounter lifecycle or pacing defect. Inspect checkpoint restore, pending waves, group completion, alive caps, visibility rejection and unreachable survivors before choosing a fix. Keep diagnostic counts/reasons useful for subsequent reports; preserve intentional quiet periods.

## Start here

These are entry points, not a prescribed edit list; verify current code before changing it.

- `packages/server/src/ai/director/spawner.ts`
- `packages/server/src/ai/director/director.ts`
- `packages/server/src/session/Session.ts`
- `packages/server/src/session/mission.ts`
- `packages/shared/src/data/encounters/mission-01.json`

## Acceptance criteria

- [ ] A deterministic regression exercises the actual cause and fails before the fix; record the seed, objective, retry sequence and observed counters.
- [ ] Fresh start, checkpoint retry, full group wipe and one surviving enemy all either deliver the configured next pressure or expose an intentional, bounded wait reason.
- [ ] An unreachable or unaware survivor cannot indefinitely suppress required later encounters; prevent duplicate waves or cap violations when recovery occurs.
- [ ] In-page and hosted sessions agree; checkpoint restoration does not duplicate or permanently consume encounters.

## Verification

Run targeted director, spawner and session regressions, then all-mission scenarios at both budgets. A three-seed mission run can report gameplay losses without failing, so inspect its diagnostics; add a specific no-stall assertion. Record a mission-01 playthrough/retry checklist.

Follow the shared [delivery workflow](../../.claude/commands/next-task.md), including `pnpm verify`. Record commands/results and observed acceptance evidence below. Visual/audio quality and owner decisions are not proven by a passing test.

## Handoff / evidence

Not started. No reproduction, implementation, playtest or owner approval is claimed.
