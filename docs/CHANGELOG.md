# Changelog

One line per completed task, newest last. Appended by whoever completes the task
(standing rule 6 in `PLAN.md` §0.3).

Format: `T-<id> — <what changed>`

---

- T-0.13 — Wrote ADR-001 through ADR-015 plus index and template in `docs/adr/`.
- T-0.01 — pnpm workspace with shared/server/client/bot/tools; root `pnpm verify`.
- T-0.02 — Strict TS with project references; `shared` resolves to source.
- T-0.03 — ESLint flat config; bans platform imports and non-deterministic Math in `shared`.
- T-0.04 — Vitest with per-package projects.
- T-0.05 — CI running `pnpm verify`, plus a separate non-V8 parity job.
- T-0.14 — Deterministic trig (committed 1025-entry quarter table) and sfc32 PRNG.
- T-0.08 — Fixed 30 Hz timestep with spiral-of-death clamping.
- T-0.09 — bitECS world, components, and never-reused NetId allocation.
- T-0.10 — Rapier `-deterministic-compat` loads in Node; cost measured at 7% over default.
- T-0.11 — Parity harness reporting bounded divergence, replacing the golden hash.
- T-0.12 — `Simulation` class and `sim-run` CLI with a `--parity` mode.
- T-0.07 — Server bootstrap: config, JSON logging, tick loop, clean SIGTERM.
- T-0.06 — Vite + Three.js client with COOP/COEP headers set from the start.
- T-1.01 - BitStream reader/writer, bit-packed, throws on over-read.
- T-1.02 - Quantization specs with documented error bounds; clamps, never wraps.
- T-1.03 - Replication schema and full snapshot encoding in wire integers.
- T-1.04 - Delta compression with per-field masks and a bounded baseline ring.
- T-1.05 - Tagged protocol messages with handshake version rejection.
- T-1.06 - Transport interface plus a queued in-memory loopback pair.
- T-1.21 - NetSim: clock-driven latency, jitter, loss and duplication.
- T-1.07 - WebSocket server transport on `ws` (see ADR-008 addendum).
- T-1.08 - Browser WebSocket client transport with exponential backoff.
- T-1.09 - Connection lifecycle, handshake validation, heartbeat timeout.
- T-1.10 - Clock sync with median RTT and offset estimation.
- T-1.12 - Kinematic character controller, pure and table-trig driven.
- T-1.13 - Authoritative 30Hz session with six slots and per-client deltas.
- T-0.06 - Client rebuilt as a local movement QA harness with live tuning.
- T-1.11 - Keyboard and pointer-lock input producing MoveInput, edge-latched.
- T-1.14 - Client prediction: immediate input with replay history.
- T-1.15 - Reconciliation with input replay and linear correction smoothing.
- T-1.16 - Remote entity interpolation, Catmull-Rom with a capped extrapolation.
- T-1.20 - Headless bot client running the real prediction path, plus CLI.
- T-1.22 - Netcode convergence matrix: 2/6 bots x latency x loss, M1's exit gate.
- T-1.12 - Fixed inverted strafe handedness; pinned with direction tests.
- T-0.06 - Orbit camera with real pitch, asymmetric limits, FPS/TPS toggle.
- T-1.17 - Hitscan weapons: data-driven defs, deterministic seeded spread, falloff.
- T-0.06 - Client becomes the QA home: live weapon range for T-1.17, tracers, falloff.
- T-0.06 - First weapon QA pass: reload loop, semi-auto, shoulder camera, ADS cue.
- T-0.06 - Weapon and camera tuning panels; every QA window collapsible.
- T-0.06 - Pitch up raised to 89 degrees; camera orients by Euler, not lookAt.
- T-1.18 - Lag compensation: rewound hitbox history, capped at 200ms, no restore step.
- T-0.06 - Muzzle moved to the character's right hip; ADS brings it up and in.
- T-1.18 - Session wiring: Fire/HitEvent messages, server-authoritative cadence, protocol v3.
- T-0.06 - Harness connected to an in-page authoritative session; link-condition sliders.
- T-0.06 - Muzzle by view: TPS shoulder, FPS hip, FPS ADS centre; trace origin split out.
- T-0.06 - Restore inter-tick render interpolation; range targets are server-side hittable.
- T-0.06 - Build stamp in the HUD; server now decays weapon bloom (accuracy fix).
- T-1.15/T-1.18 - Smoothness pass: reconcile fixes, input redundancy, hold-not-repeat.
- T-1.17/T-1.18 - Predicted tracers (no firing delay) and rewound shooter origin.
- T-0.06 - Fix TPS shots landing down-left: one complete shootable list for aim convergence.
- T-1.19 - Damage with hit zones, death and 5s respawn; health replicated and shown.
- T-0.06 - Aim rides at 1/4096; convergence limited to server-shootable geometry; spawns off the firing line.
- T-1.23 - Netgraph overlay (G) with a second in-page client; fixed a client-killing Pong encode and continuous extrapolation.
- T-1.24 setup - Per-client link conditions: your link and the sparring partner's move apart.
- T-1.24 - M1 playtest gate: human sign-off recorded in docs/playtests/m1.md. M1 closed.
- T-2.01 - Camera solve lifted out of the render loop, into a testable module.
- T-1.5.01 - Session host process: `pnpm host` serves the real Session over a WebSocket, with per-connection link conditioning and `pnpm bot --url`.
- T-1.5.02 - Client joins a real host with `?host=ws://…`; two tabs share one session. Fixed a slot reused by a new client keeping the old occupant's input tick, which silently discarded every input from the newcomer.
- T-1.5.04 - Protocol v6: `Join` carries a room code, `JoinAck` the room landed in, `Disconnect` a typed code; `Roster` message; voice-safe room codes. A v5 client is rejected on version, not on room.
- T-1.5.05 - Room registry: one host holds many sessions, each on its own tick clock; empty rooms reclaimed after a grace; room, connection and process caps reject rather than degrade; `/healthz`.
- T-1.5.06 - Lobby: host a room or join a code from the page, six-row squad panel, leave, share link; `?host=`/`?room=` pre-fill rather than bypass; default host baked in from `SANDLINE_HOST`.
- T-1.5.07 - Host container, Fly config with TLS at the edge and auto-stop, deploy workflow, `DEPLOYING.md` deploy and teardown. The deploy itself needs an account and is documented, not run.

- T-2.02 - Frame-rate-independent asymmetric spring arm: snap inward, exponentially ease outward, with derived settle-time tests.
- T-2.03 - Camera arm collision against static scenery, isolated from the server-authoritative shootable set.

- T-2.04 — Eased Q-key shoulder swap with mirrored camera/muzzle offsets and 10/95 m aim-convergence coverage.
- T-1.5.03 — Human-to-human LAN gate: owner playtest sign-off recorded; two-human combat feel accepted.
- T-1.5.08 — M1.5 internet gate: owner completed the two-human real-host playtest and reported it looks great; M1.5 closed.
- T-2.05 — ADS transition: arm length, shoulder offset and FOV now share one frame-rate-independent normalized transition.
- T-2.06 — Replace capsule player representations with a shared humanoid grey-box soldier placeholder; move E-2.1 human sign-off to T-2.07.
- Reticle: fixed at the centre again, with a gap that follows the weapon's cone — wide in hip fire, wider under bloom, tight when aiming; the moving third-person reticle (which lagged the view by a frame) is gone and camera-to-eye aim convergence is back so the centre is where shots land. Aiming still enters first person until V.
- Review of E-2.1 (T-2.02..T-2.06): the humanoid's hit root is now the server's hitbox capsule rather than the torso box, so the harness hits what the server hits; V no longer flips the shoulder on key auto-repeat; camera collider stops allocating per frame; stale HUD key hint; leftover debug logging removed from CombatQA; Host workflow fails early without GH_PAT and the docs name both secrets.
- T-1.12 — World collision: one shared box world (posts, rails, figure, cover from `data/world.json`) that the controller collides with (slide, step, land, head room), the server stops shots on, and the client renders and raycasts; parity over 500 ticks is exactly 0 on every engine. Firing-lane posts removed so the range stays shootable from every slot.
- T-2.08 — Recoil patterns: per-weapon kick, seeded drift, cap and recovery in `weapons.json`; a view offset laid over the mouse so recovery never eats the player's compensation; frame-rate-independent recovery; sliders in the weapon panel.
- T-2.09 — Camera shake: a decaying positional and roll impulse per shot, summing across a burst and bounded by its own decay; composed after the arm into separate solve fields so the aim never reads it; per-weapon magnitudes in data and a shake slider (0 = off).
- T-2.10 — Muzzle flash and shell ejection: an additive sprite and a point light at the visual muzzle for two frames, and a box shell thrown right and back on a closed-form arc that lands, rests and fades; both pooled once and capped, so a held trigger never allocates; live counts on the HUD.
\n- T-2.11 — Impacts: a scenery stop (hit event on netId 0 whose point lies on a face of the shared world) leaves a dark mark on that face and throws sparks from the server's point; a soldier hit flinches; max-range misses and the ground, which the server has no box for, draw nothing. Pooled and capped with the other effects.\n
- M1.5 verdicts written down: `docs/playtests/m1.5-lan.md` (T-1.5.03) and `m1.5.md` (T-1.5.08), after the fact from the record, stating what each gate does and does not establish; ADR-012 addendum closes the two-human re-gate; §10 item 1 done except the netgraph numbers, still owed.
- T-2.12 prep — `docs/playtests/e2-4.md` run sheet (not a verdict; says so); weapon panel gains shake sliders and its paste-back block now carries the recoil and shake fields it was dropping, held to the type by a test.
- E-2.6 broken out (T-2.13..T-2.16). T-2.13 — Downed state: zero health downs rather than kills; a bleed-out timer in `damage.json` that damage cuts in proportion; crawl speed and a `downed` gait the server imposes and the predictor mirrors; downed and dead cannot fire; vitality and its countdown replicated in `Health` (protocol v7); HUD shows DOWNED / DEAD with the server's timer.
- T-2.14 — Downed presentation: the humanoid's parts lie down on the hit capsule, which does not move; the camera pivot eases to a crawl height; remote downed soldiers are posed from replicated vitality; the reticle hides and a DOWNED banner counts the server's timer.

- T-2.07 / T-2.12 / T-2.16 — Human sign-offs passed for E-2.1 camera, E-2.4 weapon feel, and E-2.6 downed/revive; M2 moves to E-2.2.
- Planning: broke E-2.2 into T-2.17 through T-2.23, covering locomotion state, eight-way gait, remote playback, crouch height/hit volume, authoritative vaulting, vault presentation, and human sign-off.

- T-2.17 — Added a pure rendered-velocity locomotion classifier with idle/walk/sprint/crouch-walk/crawl states, eight-way direction, normalized gait inputs, and QA HUD integration.
- Planning update 2026-09-20 — E-2.2 now explicitly includes T-2.22, a reusable humanoid character model/rig integration before the human gate; the grey-box remains only as a fallback/diagnostic fixture, and the E-2.2 sign-off moves to T-2.24.
- T-2.18 — Procedural eight-way grey-box gait: continuous blend of forward/back/strafe leg and arm poses across the eight movement directions with a gait phase that does not snap at direction boundaries; walk and sprint use distinct stride magnitude/rate, idle settles exactly to the rest pose, and the driver is shared by local and remote soldiers. (Backfilled — this task was completed but never logged; PLAN.md carries no completion note for it either.)
- T-2.19: integrate local predicted and remote interpolated locomotion playback through the shared pose driver.
- T-2.20 — Added authoritative crouch height/clearance, crouched humanoid presentation, and crouch-aware lag-compensated hit volumes.
- T-2.15 fix — a revive hold survives idle ticks: the interact button is latched from the newest real input instead of read off the tick's input, so gaps in a bursty input stream pause the hold rather than resetting it; silence past the repeat window still releases it. Tests: inputs every third tick complete on time; a silent reviver drops the lock.
- T-2.21 — Authoritative vault: obstacles taller than a step and no taller than 1.25 m are vaulted on forward intent; a closed-form traversal in movement config (distance, seconds, lip), server-owned and mirrored exactly by the predictor from the replicated `Vault` component (protocol v10); no vault while downed, crouched, firing, jumping or airborne, and no Fire mid-vault; the parity harness vaults a hurdle.
- T-2.22 — Skinned humanoid soldier built in code (one `SkinnedMesh`, seventeen bones, one draw call) replaces the grey box as the local and remote presentation, behind a rig contract (`humanoidRig.ts`: named bones, chest-mounted aim attachment carrying the rifle, per-pose base transforms, gait style) that the grey box also implements (`?greybox`); the pose driver now layers the gait on the pose, with knees, hip bob and chest twist on the skinned rig; the hit root is unchanged.
- T-2.23 — Vault presentation: a `vault` locomotion state driven by the authoritative progress (local prediction and remote interpolation alike; the interpolation buffer now carries `vaultElapsed`), a vault pose curve on any rig with lead/trailing legs and a chest lean, gait crossfaded in and out and a landing dip, and a per-frame joint-step readout in the HUD that proves no snap.
- Review fixes (T-2.15, T-2.20) — a reviver holds one revive at a time: with two downed teammates in reach, one held E no longer brings both back in a single hold; and standing up while jumping under a ceiling clamps the feet to the standing height instead of lifting them 0.6 m for a tick.
- T-2.24 prep — `docs/playtests/e2-2.md` run sheet (not a verdict; says so) for the E-2.2 sign-off on the skinned soldier: figure at rest, eight-way gait, crouch, jump, vault, crawl and revive, the other person over the host, and the grey-box fixture; the movement panel gains crawl speed and the vault's seconds, distance and maximum height.
- Planning: broke E-2.3 into T-2.25 through T-2.29 (aim offsets, fire and reload layers, hit reactions on the rig, foot placement, sign-off), each a procedural layer on the rig contract; refreshed §10.
- T-2.25 — Aim offsets: the rig contract's `aimAt(pitch, weight)` layer pitches the spine, neck and rifle to the aim and re-solves the hands onto the grips every frame, bit-exact at level; locally from the view pitch (recoil included), for remotes from the replicated `Transform.pitch`, now carried through the interpolation buffer; fades through a vault, off while downed.
- T-2.26 — Fire and reload layers: `hold(state)` on the rig contract takes aim, kick and reload in one pass; the kick (`weaponKick.ts`) drives the rifle back and up by the weapon's kick in data and recovers frame-rate independently, locally from the predicted shot and for remotes from the server's shot event; the reload is a curve of the weapon's clock with the left hand to the magazine well and back; a `Weapon` component (index, reload percentage) rides the snapshot (protocol v11).
- T-2.27 — Hit reactions on the rig: `react(reaction)` on the rig contract turns the chest away from the shooter and tilts it along the shot, with a head snap on a head-zone hit, scaled by damage and recovered on the T-2.02 curve as a closed form of the age; direction and zone come from state every client already has (`hitReaction.ts`, the shot's shooter and the impact's height against `damage.json`'s fractions), `effects.flinch` becomes the entry point that asks the rig, and the grey box keeps the T-2.11 translation flinch; a downed soldier does not react.
- T-2.28 — Foot placement: each foot reads `supportUnder` at its own x/z in the shared world and a two-bone leg IK plants it, bounded, with the hips settling to the lower foot and the other knee taking up the difference; the gait stays on top (its knee is the pole hint, and a foot it has lifted is not planted), the settle is a critically damped spring so no joint moves faster than the walk already does, and flat ground is the pose driver's own bits. The arms' and legs' IK is now one solver (`twoBoneIk.ts`). Off while airborne, vaulting or downed; the root is never touched. The harness HUD gains a feet readout (where you stand, each foot's support, the hips' settle).
- E-2.5 broken out (T-2.30..T-2.34: ballistics, projectiles on the wire and on the server, the throw in the page, detonation presentation, sign-off) with an ADR-005 addendum recording that projectiles, like the character controller before them, are pure arithmetic over the shared box world rather than Rapier — the player aims by the arc they are shown, so the preview and the authoritative flight must be one function over one list.
- T-2.30 — Ballistic arcs: `ballistics.ts` and `projectiles.json` (a frag grenade and a rocket, validated at import like the weapons), a step that integrates the parabola and sweeps the projectile's sphere along the chord so nothing tunnels, bounces off the entry face, lands-and-skids rather than sticking, and detonates on fuse, impact or old age; `rayWorld` gains an inflation radius and the face normal it entered through; a blast falls off from a flat core to an edge fraction and is cut by three probes' worth of cover. Pure arithmetic, bit-identical on a re-run, no clock read.
- T-2.31 — Projectiles on the wire and on the server: a `Throw` message (untrusted like `Fire`; the pouch and the cooldown are the server's), projectiles as replicated entities that spawn, fly and despawn — the first exercise of the delta format's spawns and despawns since T-1.04 — carrying a new `Projectile` component (kind, owner) beside their transform and velocity (protocol v12); they step last in the tick so a rocket meets the bodies where it left them and a `Detonation` names the tick of the snapshot they vanish from; the blast goes through `applyDamage` like a bullet and catches everyone in reach, the thrower included. Incidental fixes: `isRangeTarget` is a bounded range rather than "anything above 1000", and `applyFire` no longer stores a table-unit pitch in the slot's wire-unit field (a remote's rifle flicked for a tick after each shot).
- T-2.32 — The throw in the page: hold G to see the arc (`projectileArc` itself, over the world the server flies the real one through, drawn into a fixed buffer), release to throw; 5 and 6 pick from the pouch and the netgraph moves to N. `ThrowQA` owns the local pouch, cooldown and predicted ghosts with no THREE in it; the ghost is bound to its replicated twin by kind and owner slot when the server's copy arrives, the twin is hidden while the ghost lives and the ghost retires when the twin leaves the world, so one grenade is on screen throughout and the blast stays the server's. `NetClient` keeps projectiles in their own interpolation buffers and holds each `Detonation` until the render clock reaches the tick it names.
- T-2.33 — Detonation presentation: a pooled blast (fireball, light, debris on the shells' own ballistic arcs, and a scorch disc on whatever is under it), every part a closed form of its age so any frame rate draws the same picture; the camera's jolt is scaled by `blastDamageOn` itself, so cover and distance cut the shake exactly as they cut the damage; each target takes the T-2.27 reaction away from the blast; and `NetClient` holds each detonation until the render clock reaches its tick, so a blast never goes off in front of a grenade still visibly in the air. The netgraph's key is now N.
- Planning: §10 refreshed — E-2.2, E-2.3 and E-2.5 are all built and waiting on human gates (T-2.24, T-2.29, T-2.34), which want the same two people on the same host and can be judged in one sitting; E-2.7 is the last M2 epic and the one that needs audio assets decided.
- Review fixes (T-2.31, T-2.32) — a session carries at most 24 projectiles at once (a bandwidth rail, well above what the pouch and cooldown allow, and a refused throw spends nothing); a throw key released with no session to throw into no longer opens the next session with a grenade.
- Planning: broke the soldier's look into T-2.35 through T-2.39 (§7.6) from `docs/HANDOFF-SOLDIER-LOOK.md` — one generated diffuse atlas, the silhouette, the era's shading, squad colours, and a human sign-off. The era was already the locked art direction (PLAN.md line 5, ADR-013's context); what never existed was any of the treatment. PS2 and not PS1: no vertex jitter, no affine warping. Nothing authoritative moves.
- T-2.35 — One generated diffuse atlas: `soldierTexture.ts` paints a 256² `DataTexture` from arithmetic alone as a 4×4 grid of 64px cells (face, helmet, torso front/back, vest, belt, sleeve, glove, trouser, boot, neck, rifle, pack, two plain), point-magnified and sRGB, with the palette in `soldierPalette.json` (rule 4). A `DataTexture` and not a canvas because client tests run in Node where a canvas throws; not an image because this client has no loader, no `public/` and no asset pipeline until E-4.1. `remapGeometryUv` moves each primitive's UVs into its cell and takes an array to give a box a cell per face, so one torso box carries a placket and pockets on the front and a plain yoke on the back; `weld()` carries `uv` across and the flat per-segment vertex colour is gone. Two measured findings are pinned by test: Three's spheres and capsules emit `u` outside [0,1] (-0.0625..1.0625 on an eight-segment capsule) so `cellUv` clamps, and the remap insets by half a texel or a primitive's last column reads the next cell. Geometry, bones, bind pose, hit capsule and aim attachment untouched; the rifle shares the atlas and stays the second draw.
- T-2.36 — The silhouette: era-correct proportions inside the same 1.8 m capsule. A helmet with a BRIM (one disc at the dome's rim — the single strongest era cue the model has; without it a helmet reads as a swimming cap), oversized boots with an upper on the shin that wears them so the ankle is attached to something, bigger gloves, thicker limbs on six radial segments instead of eight, shoulder slabs instead of balls, and a collar and five pouches as geometry, because gear that breaks the outline is most of what tells a 2002 soldier from a mannequin. **Triangles went DOWN, 1,446 to 980**: faceting the limbs bought more than the gear cost. Every joint position in the bone table is unchanged, so the arms' IK, the legs' derived lengths and every pinned assertion hold as they were, and all of E-2.2's and E-2.3's layer tests pass untouched. New guard: every skin vertex stays within `HUMANOID_HIT_RADIUS` of the root axis and fills at least 80% of it — a shoulder or pack outside the capsule is a netcode bug wearing art's clothes, since you would watch rounds pass through visible kit.
- T-2.37 — The era's shading: the characters drop PBR for `MeshLambertMaterial` (diffuse and nothing else, which is what hardware lighting in 2002 was, and cheaper besides) and the shadow filter hardens from `PCFSoftShadowMap` to `PCFShadowMap` — a soft penumbra under a soldier and a roughness response are the two things that read as modern however the character is textured. The grey box takes the same material so the fixture is lit like the thing it stands in for. **Smooth normals on purpose:** `flatShading` is the reflex and it is the wrong console — the PS2 interpolated per-vertex lighting across a triangle, so curved surfaces read smooth and only the silhouette gave the polygon count away; faceted shading is a 2015 indie look. No vertex jitter and no affine warping, both of which are PS1. ADR-013's one sun and the dust haze are untouched. The low-resolution render target the handoff floated is NOT done and is deferred to T-2.39: it trades crosshair, tracer and hit-marker legibility that the two open feel-gates are judged on, and that is an owner's call.
- T-2.38 — Squad colours: the palette data becomes a `base` plus per-name overrides, so a squad of six is one set of colours wearing six markings rather than six unrelated schemes — and a slot colour is one line of data. `paletteFor({local, human, slot})` reads the palette off the roster; `setSoldierPalette(root, name)` repaints a LIVE soldier by swapping the atlas only — same geometry, skeleton, material and draw call — so a slot flipping between bot and human changes colour on the entity already standing there, which is ADR-001's swap made visible. `main.ts` re-asks every frame and the call skips when nothing changed. The marking moved twice for a reason worth recording: the slot's colour is now the HELMET BAND, because a patch does not survive forty metres, and the band sits a third of the way UP the dome rather than at the rim, where T-2.36's new brim occludes it exactly — the first version was painted correctly and invisible. Shoulder patches carry it too, since the helmet mark and vest patch both face front and a squad seen from the side shows neither. `remote` stays as the fallback before the roster arrives.
- T-2.39 prep — `docs/playtests/soldier-look.md` run sheet (not a verdict; says so) for the soldier-look sign-off: whether it reads as 2002 rather than as untextured geometry, the silhouette from every side, the look through E-2.2's and E-2.3's layers, telling six slots apart at 40 m, a firefight rather than a showroom, the grey box still rendering, and performance. Section 6 carries the one decision T-2.37 left open — the fixed low-resolution render target — because it trades crosshair and tracer legibility that T-2.24 and T-2.29 are judged on. `PLAN.md` §7.6 and the README now record the pass as agreed and built; the handoff doc is marked superseded and kept for its reasoning.
- B-01 — Fixed the strafing tracer bug: a shot's `HitEvent` now carries the shooter's own rewound origin (`originX/Y/Z`, PROTOCOL_VERSION 13), the same point `resolveShot` already traced from server-side, instead of leaving every OTHER client to approximate one from the shooter's current replicated position. That approximation lagged a strafing shooter's true muzzle by however far they moved during the round trip plus the interpolation delay — a small angle at range, but at close quarters the same absolute drift is a large fraction of the shot's own length, which is what read as a tracer running sideways rather than along the shooter's aim. Confirmed with two headless clients over a real host under 150 ms latency: the drawn origin tracked the shooter's actual position to the wire's quantization instead of trailing 1.5–2 m behind it.
- B-02 — Fixed the run-and-fire strobing bug: `LocalInput` gains a `bodyYaw` getter that is the mouse's own accumulated yaw with recoil's view kick (`offsetYaw`) left out, and `main.ts` now classifies the local player's locomotion against `bodyYaw` instead of `yaw`. Each shot's recoil kick carries a randomized sideways sign (`weapons/recoil.ts`), and `yaw` (which does carry the kick) was feeding `classifyLocomotion`'s `facingYaw`, so `directionAngle` — and the gait's `forward`-scaled twist, lean and knee-bend terms in `locomotionPose.ts` — jittered sign shot to shot. That read as the run animation strobing while firing, worst while moving straight forward or backward where those terms sit near their peak amplitude. `yaw` itself (sent on the wire, and what aims the camera) is unchanged, so aim and server-side facing still see the recoil kick as before.
- B-03 — Fixed the floaty, over-long grenade throw: `projectiles.json`'s frag retuned from 17 m/s at 8° under real 9.81 m/s² gravity with 0.15/s drag (a flat, fast line-drive that sailed ~17.5 m past the landing skid) to 12 m/s at 8° under 13 m/s² gravity with 0.4/s drag — same stepper and same arc-preview path (`ballistics.ts` untouched), just heavier numbers, so a level throw now lands in well under a second and settles around 9 m rather than gliding to 17.
- B-04 — Closed as stale: "rocket launcher cannot be equipped or fired" predates E-2.5's projectile work. `RL-2 Rocket` has been equippable (key 6, pouch slot 1) and throwable (hold G, release) since T-2.31/T-2.32 landed; confirmed with a browser run against `pnpm host` — selecting the rocket, holding and releasing G spends one from the pouch and detonates it, same as the frag. No code change; the bug report was never updated after the epic that fixed it shipped.
- B-05 — Fixed the downed-state crawl regression: `stepCharacter` (`CharacterController.ts`) now gives a downed soldier zero speed instead of `crawlSpeed`, so no input produces movement while down; `crawlSpeed`/`'crawl'` are removed end to end (`MoveConfig`, `LocomotionSpeeds`, `LocomotionState`, the tuning panel's slider, HUD/comment text) since there is no longer a crawl gait to drive. The local camera (`main.ts`'s `solveCamera` call) now forces third person while downed regardless of the player's FPS/TPS preference, and the local body stays visible rather than hidden by the first-person check. Server and client predictor already forced `input.downed` from vitality, so both sides pick this up for free.
- B-05 follow-up — Reworked the downed pose (`humanoidSoldier.ts`'s `DOWNED`) from the flat, symmetric arms-out scarecrow to an asymmetric one: a `chest` roll about its own long axis lifts one shoulder clear of the ground while the other settles toward it (a rotation about that axis leaves the head/neck chain's position exactly unchanged, so the existing head/foot/facing assertions hold bit-for-bit), and the near arm folds inward and up to rest a hand on the abdomen instead of splaying open at the side like the far arm. Tuned against the actual bone-world-transform math (`worldOf` in the test, not eyeballed) and pinned with two new assertions: the shoulders' world-Y differ by more than 8 cm, and the clutching hand sits more than 30 cm higher (world Y, yaw-invariant) than the resting one.
- Planning: ADR-016 reopens ADR-002's prone-stance exclusion — voluntary prone/crawl for a standing, alive soldier (not the downed crawl B-05 just removed, which stays removed) is back in scope as E-2.8, built on the crouch (T-2.20) and crawl-gait (E-2.6) groundwork rather than the from-scratch animation cost the original exclusion priced in. ADR-002's table is corrected to point at it. Leaf tasks T-2.40..T-2.43 broken out in PLAN.md §7.8 and tracked in TASKS.md; T-2.40 is open, the rest blocked on it.
- B-06 — Fixed browser shortcuts firing during play: `LocalInput`'s keydown handler only called `preventDefault` for Space, so with the mouse captured (`this.locked`), a key pressed alongside a held Ctrl (crouch) or other modifier reached the browser as a shortcut (find, bookmark, print, ...) instead of the game. Now every keydown is prevented while the mouse is captured, not just Space. A handful of browser-reserved combos (Ctrl+W, Ctrl+T, Ctrl+N, ...) can't be blocked by `preventDefault` at all — that's a browser security restriction, not fixable client-side without the Fullscreen + Keyboard Lock APIs, which are out of scope here.
- B-06 follow-up — Crouch moved off Ctrl entirely: `preventDefault` on every keydown while the mouse is captured stops most browser shortcuts, but Ctrl+W is one of the handful the browser reserves and blocks from page script no matter what, and Ctrl was bound to crouch — so holding it to crouch-walk forward closed the tab. Crouch (`LocalInput`) is now a toggle on C, same shape as V's shoulder toggle in `viewState.ts`: press once to crouch, press again to stand, auto-repeat ignored. `MoveInput.crouch` and everything downstream (locomotion, hit volume, pose) is unchanged — only how the local player's intent is captured moved. Planning: PLAN.md §8 gains R13, a standing rule against ever putting Ctrl/Alt/Meta in a held combo with another game key again.
- R13 follow-up — Fullscreen + Keyboard Lock implemented: `packages/client/src/input/keyboardLock.ts` arms `navigator.keyboard.lock()` via a `fullscreenchange` listener the moment `LocalInput`'s canvas becomes the fullscreen element, and `unlock()`s on any exit (Esc held, F11, the browser's own UI) — the listener reacts to the browser's own event rather than every exit path being handled by hand. The same click that captures the mouse now also requests fullscreen on the canvas (one user gesture covers both, which both APIs require), skipped entirely where `navigator.keyboard.lock` doesn't exist (Firefox, Safari) since fullscreen with no lock behind it is a jarring UI change for nothing. A refused lock (no user activation, one already held) falls back to ordinary `preventDefault` rather than breaking input. `LocalInput.immersive` and the HUD's "mouse captured" line ("shortcuts locked out") surface the state for QA. Unit-tested against a faked `navigator`/`document` (Node has neither API); `pnpm verify` green.
- T-2.40 — Prone: a third stance in `stepCharacter`'s ladder (standing → crouched → prone), generalizing the crouch ratchet — drop a level instantly, rise one level at a time only once clearance at the target height exists — rather than adding a second parallel boolean beside `crouched`. Its own config (`proneHeight` 0.8 m, `proneSpeed` 1.1 m/s) and its own server hit-capsule (`DEFAULT_HITBOX.proneHalfHeight`/`proneCenterOffsetY`, 0.8 m total — under crouch's 1.2 m and standing's 1.8 m), selected through a new `capsuleFor` helper shared by `resolveShot` and `Session.ts`'s `bodyAlong` instead of a third copy of the crouch ternary. Replicated on the existing `Crouch` component as a second bit (protocol v14) and a new `INPUT_BUTTONS.prone`. Jump is blocked while prone; vault refuses to start from it, same as crouch. Not the downed crawl B-05 removed: prone is voluntary, keeps the weapon, and is forced off whenever downed takes over. `pnpm verify` green.
- T-2.41 — Prone presentation: a `prone` `HumanoidPose` on both rigs (grey box and skinned soldier), reusing the crouch/downed pose-offset contract rather than a new one. The turn is the opposite pitch from downed's (`Euler(PI/2, 0, 0, 'YXZ')` vs downed's `Euler(-PI/2, PI, 0, 'YXZ')`), so the body lands face down instead of on its back, with the weapon still held (arms keep the standing IK-solved hold) and `aim.visible` left `true` — only `downed` hides it. A `prone` `LocomotionState` (classifier and gait) gives it a slow crawl gait, scaled down from crouch-walk's the way crouch-walk is scaled down from a walk, and idle-prone restores to exactly the prone pose the same way idle-crouch already does. The camera gained a `proneBlend`/`proneEyeHeight`, eased on the identical curve `downedBlend` already uses, so the pivot drops to a lower firing height without cutting. Tests cover both rigs' pose application/restoration onto standing and crouch, structural distinctness from downed in the same suites that guard it, the classifier and gait, and the camera curve. `main.ts` wiring (replicated stance → `setPose('prone')`) is out of this task's file list, same as the pose modules it depends on; `pnpm verify` green.
- M3 breakout — PLAN.md §7.9: M3's nine epics broken into 37 leaf tasks (T-3.01..T-3.37, five 🧍 gates, one ⚠️ spike), ahead of M2's exit gate at the owner's request; five rules (AI drives `MoveInput` through `stepCharacter`, server-only and seeded, 10/30 Hz, data over code, competence measured headless before it is judged) and four scope calls recorded for the owner (rifleman + MG only per ADR-015, unclassed order authority per ADR-001, a minimal mission not E-4.4, a proposed ≤25 % AI tick budget). `TASKS.md` gains the M3 tables; T-3.01, T-3.02 and T-3.07 are OPEN. Planning only — no code.
- Fullscreen HUD fix — clicking in now fullscreens the page root (`document.documentElement`) instead of the canvas, so the HUD, crosshair and downed overlay (the canvas's siblings) stay visible; pointer lock stays on the canvas. Held Ctrl crouches again, but only while fullscreen, where Keyboard Lock keeps Ctrl combos inside the game; the C toggle is unchanged. `pnpm verify` green.
- Prone wiring fix — `main.ts` now drives the T-2.41 prone presentation that was left unwired: holding Z sets the local rig to `setPose('prone')` (prone beats crouch, standing through a vault), remote rigs read the replicated `sample.prone` bit, both locomotion classifiers get `prone` so the crawl gait plays, and the camera gets `prone` so the pivot eases to `proneEyeHeight`. Previously prone was authoritative on the server but invisible in third person; `pnpm verify` green.
- FPS stance camera — prone eye height lowered 0.5 → 0.4 m, and crouch now drops the pivot to a new `crouchEyeHeight` (1.15 m, matching the pose's 0.4 m hip drop) on the same eased curve; prone beats crouch so the drops never stack, and a vault or downed state ignores crouch. Applies in both views, as prone already did. `pnpm verify` green.
- Vault on jump — `stepCharacter` now starts a vault only when jump (Space) is pressed at a vaultable obstacle with forward intent; walking into a hurdle without jump stops at it like a wall, and jump with nothing vaultable ahead is an ordinary jump. Server and predictor share the rule, so no wire change. Tests updated to press jump at the obstacle, plus two new cases; `pnpm verify` green.
- Prone pose fix — the skinned soldier's prone spine bent toward its front (the ground, after the hips' face-down turn) and drove the head through the terrain. The chain now arches back onto the elbows, the neck and head take the rest of a quarter turn so the face looks along the facing, and the hips sit 0.1 m lower so the belly meets the ground. The rifle gets a per-pose rest: prone lays it forward along the ground, butt at the right shoulder, arms re-solved onto it with elbows planted; the aim layer composes pitch on that rest. Regression test covers head height/facing, no skin below ground, and rifle direction. `pnpm verify` green.
- Stance input — Z now toggles prone instead of holding it, exclusive with the C crouch toggle (Z from crouch goes prone, C from prone goes to crouch). Pressing Shift (sprint) or Space (jump) stands up out of a toggled crouch or prone; the Space press that stands you up doesn't also jump. Client-only (`LocalInput`), no wire or sim change; `pnpm verify` green.
- B-02 (reopened, refixed) — Reverted PR #44's `bodyYaw` change, which never fixed the run-and-fire strobe: the body it re-classified is hidden in first person, where the bug was reported. The real cause was the muzzle flash. Shots are taken on the tick, from the character's tick-end position, but the camera renders from a position interpolated up to a tick behind that; the aimed first-person muzzle is the eye, and the flash sits `FLASH_FORWARD_M` (8 cm) in front of it, just inside the 0.1 m near plane. Standing still, that keeps it clipped out every frame. Running forward or backward, the tick-to-frame gap runs straight down the view axis, so the flash landed in front of the near plane on some frames and behind it on others, and each round blinked a screen-filling sprite on or off. Strafing moves sideways, not along that axis, which is why it didn't strobe. Fix: `main.ts` queues each shot's flash and shell and draws them from the muzzle as it is drawn in that frame (the rendered position, at the camera's own pivot height). `WeaponEffects.followMuzzle` keeps live flashes on that muzzle for their two frames. Shells now inherit the shooter's velocity, so a shooter running backward can no longer overtake their own brass into the middle of the view. Hits are still traced from the tick's eye, and tracers are unchanged. `pnpm verify` green.
- B-07 — Frag un-weighted after B-03 overshot: `projectiles.json` goes from 12 m/s under 13 m/s² gravity with 0.4/s drag to 14 m/s, 11 m/s², 0.28/s (loft unchanged). Measured with `projectileArc` from eye height: a level throw lands at ~9 m and settles ~11.5 m (was 6.7/8.3), and the best lob settles ~16 m (was ~10.6) — between B-03's too-heavy arc and the original 27 m line-drive.
- B-08 — Grenade and rocket are equipped like guns and used with the trigger. 5/6 now EQUIP (an empty pouch slot stays on the gun); with a frag in hand, hold LMB to aim (the arc is drawn) and release to throw; with the rocket, click fires and RMB shows the arc. The last one thrown switches back to the gun. G is kept as a quick-throw of the selected item. The logic is `weapons/pouchTrigger.ts` (only a press made with the item in hand can throw it, and a release lost to a blur cancels rather than throws); `LocalInput` gains a latched `consumeTriggerRelease`. New reliable `Equip { item }` message (loadout index: guns then pouch; PROTOCOL_VERSION 15), and the `Weapon` component gains a 2-bit `pouch` field, so other players see a grenade or launcher in hand before it is used. The server's `heldProjectile` is presentation only — Throw is still gated by its own pouch and cooldown.
- Weapon in hand, first and third person — `weapons/weaponModels.ts` builds one primitive model per loadout id (carbine, scoped marksman, pump breacher, sidearm, frag in the right hand, launcher on the shoulder) in the rig's aim space, each with its own grips and sight point. `HumanoidRig.setHeld(id)` swaps the model on the aim attachment (built lazily, so a carbine-only soldier is still two draws; the carbine is still the atlas-textured rifle) and moves the IK grips; local and remote soldiers use it, remotes from the replicated `Weapon` component. First person gets a viewmodel (`weapons/viewModel.ts`): the same model plus gloved forearms, in its own scene and fixed-FOV camera drawn over a cleared depth buffer (no clipping through walls, ADS zooms the world but not the gun), easing hip to ADS with the sight on the view axis, and carrying the fire kick, reload dip, walk bob, a lower-and-raise on switch, and a grenade wind-up while aiming.
- T-2.42 — Fire from prone: prone was never refused, but its shots traced from standing eye height (1.55 m, 0.75 m above the 0.8 m prone hit volume), so a body behind cover fired over it. `eyePosition` takes the body's prone stance and uses the muzzle rig's new `proneEyeHeight` (0.4 m); the server reads the shooter's stance at the same rewound instant as its origin, and the client converges aim from the same eye. Spread gains a per-weapon `proneSpreadScale` in `weapons.json` (carbine 0.6, marksman 0.5, breacher 0.85, sidearm 0.8), a stance input to `currentConeUnits`/`tryFire` beside `ads`, fed from the predicted stance on the client (tracer, reticle gap) and the rewound one on the server; the weapon tuning panel lists it. Wire tests: a prone Fire resolves and hits with a prone-eye origin, standing up restores the standing origin, and a prone magazine groups tighter by the row's scale (worst miss 0.0375 → 0.0221 rad). Shared tests read the tuning from the row, not a branch. Recoil is unchanged. Crouch and throws still use standing eye height (B-09). `pnpm verify` green.
- T-2.43 prep — `docs/playtests/e2-8.md` run sheet (not a verdict; says so) for the E-2.8 prone sign-off: getting down and up (Z/C/Space/sprint), prone vs crouch vs downed side by side and at range, eight-way crawl and whether 1.1 m/s is earned or crippling, cover on the range (hides a prone body; too tall to shoot over from prone), firing prone with the per-weapon `proneSpreadScale`, and the remote view under Poor/Awful links. Flags B-09 as known and out of scope, and names what the range cannot show (knee-high cover, a low ceiling, uneven ground). T-2.43 stays OPEN.
- T-3.01 — Recast in both runtimes (⚠️ spike, succeeded): `@recast-navigation/core` + `/generators` 0.43.1 pinned (generators in tools only). `server/src/ai/nav/NavMesh.ts` wraps Detour down to nearest point, corridor + straight path, and mesh raycast, behind an idempotent `initNav()`; `tools/src/nav/bake.ts` bakes a box soup offline. `pnpm gen:nav-spike` commits one exported mesh; `NavMesh.test.ts` loads those bytes in Node and in Chromium, Firefox and WebKit (new `nav-browsers` project in `vitest.browser.config.ts`; CI installs Chromium too) and all four path the same 47.0185 m route round the wall — divergence 0 m, bound 1 cm. `SessionHost.start` and new `LocalServer.create` await init before a session can tick, and `LocalServer`'s constructor refuses to run before it. `pnpm bench:nav`: init 61 ms, bake 86 ms, 28.6 µs/path, 2.1 µs nearest, 3.2 µs raycast; per-engine numbers in the ADR-006 addendum. New `tools` vitest project. `pnpm verify` green.
- T-3.02 — Named worlds: `data/world.json` → `data/worlds/range.json` with an `id` and a `generate` list (posts, rails, figure belong to the range, not every world); `loadWorld`/`getWorld`/`requireWorld`/`WORLD_IDS` in `world.ts`, and `DEFAULT_WORLD` is still the range box-for-box. `Session` is built with a world (id or value) and collides, shoots, throws and blasts against its boxes; `WORLD=range pnpm host` reaches every room through `Registry` and shows in `/healthz`. `JoinAck` carries the world id (protocol v16); the client builds it from the same data, predicts and draws against it, and refuses an unknown id with the new `unknown world` disconnect code. `Predictor` and the bot take the world too. Tests: a one-wall fixture world stops a walk, a shot and a grenade the range lets through; JoinAck round-trip; unknown-id refusal; range equals the old composition. `pnpm verify` green.
- T-3.03 — The box world, baked and committed: `tools/src/nav/bake.ts` triangulates a named world (floor + every box, tops walkable, sides walls) and bakes it for an agent derived from `MoveConfig` and the hitbox (radius 0.35, height 1.8, climb 0.45; 0.1 m voxels). `pnpm gen:nav` writes `server/src/ai/nav/baked/<world>.ts` (base64, agent, sha256 of world + agent + Recast config) and `index.ts`; the range is 169 kB, re-runs byte-identical; `bakedNav.ts` loads one. World files gain an optional `floor.halfExtent` (range: 100 m, so the 95 m target is on the mesh). `nav.test.ts`: bakes are fresh against the live data, and go stale on an edited box, step height or capsule; every spawn and target is on the mesh; paths reach the far target and both sides of both long walls and never cross a box at knee height (`rayWorld`); a 0.6 m gap is not walkable, a 1.2 m one is. `pnpm verify` green.
- T-3.04 — Vault links: `vaultLinks` in `tools/src/nav/bake.ts` finds every vault by asking the controller's own `tryStartVault` from just off each face of each box, one-way per side; `bakeWorld` keeps the links with both ends on the mesh (two-pass bake) and bakes them as off-mesh connections flagged walk|vault. The range gets 6, all across the low wall, three each way. `NavPath.vaults` marks vault legs for path following; `NavMesh.links()` lists what is baked. The agent and the staleness hash now cover the vault parameters and the link search settings. Tests: over the low wall 3.02 m vs 6.16 m round; every link on the mesh at both ends and a vault the controller would start; no link on the 2.4 m walls or a box 5 cm over vault height; retuning the vault makes the bake stale. `pnpm verify` green.
- T-3.05 — Path following as input: `followPath` (`server/src/ai/locomotion/followPath.ts`) turns a navmesh path, the soldier's `MoveState` and an intent (goal, walk/sprint/crouch) into a `MoveInput` for `stepCharacter`, pure and stepped at 30 Hz; `PathFollower` plans with Detour only on a new goal and when stuck, never mid-vault. Yaw toward the next corner in integer wire units, corner radius as smoothing, last step scaled to land on the point; a vault leg faces its link, stands, and jumps only when a trial `stepCharacter` starts the vault (so a crouched or firing bot never hops), and ends when the vault lands. Tuning in `follow.json`, validated by hand. `NavMesh.resolvePoint` widens the nearest-point query until the hit is truly nearest (crate-c's centre: 1.00 m, not the default box's 1.16 m; a goal past the floor's edge now resolves); `path()` uses it when given a search. Tests: fixture walks (vault once, no hop, crouch released before the jump, leg left on landing, exact arrival, stuck after `stuckTicks`); a `Session` bot slot 60 m from spawn over the low wall in 441 ticks (bound 497), replayed through `stepCharacter` exactly; a teleport behind west-wall-a repaths and arrives (and without repathing stays pinned); an off-mesh goal resolves to its nearest point. `pnpm verify` green.
- B-10 — Smooth mouse look: the camera solve no longer rounds yaw/pitch to 1/1024 turn (0.35° steps, ~2 px of mouse). `LocalInput.viewYaw` is the unrounded yaw; `cameraSolve.ts` wraps fractional wire angles (`fineTableAngle`) and interpolates the shared trig table (`fineSin`, exact at whole angles). The wire, the simulation and remote characters are unchanged. Tests: sub-unit yaw and pitch move the view proportionally; `fineSin` matches the table at whole angles and `Math.sin` within 1e-5 between. `pnpm verify` green.
- T-3.06 — Local avoidance: `Avoidance` (`server/src/ai/locomotion/avoidance.ts`) runs Detour's crowd for avoidance velocities only — every soldier's position and walked velocity written in from its `MoveState` each tick, each bot's path-following input requested as a velocity, and the velocity the crowd settles on (obstacle-adjusted, plus its push apart of overlapping agents) turned back into move axes against the bot's facing; `stepCharacter` still moves everyone. Humans are crowd entries with no request and no avoidance of their own; vault legs pass through unsteered. Detour has no right of way, so a bot held under half its asked speed for `patienceTicks` walks on unsteered until clear (without it 4 of 6 bots are still deadlocked in the west doorway after 299 ticks). Tuning in `avoidance.json`, validated by hand; `NavMesh.crowd()` and `PathFollower.onVault` added. Tests: six bots through the 1.2 m west doorway from both sides arrive at walk/sprint/crouch within bound, overlap past 5 cm at most 10/7/22 ticks (N 15/10/34), replayed through `stepCharacter` exactly; a bot passes a standing human at 0.84 m (0.06 m without avoidance); 45 agents cost ~0.4 ms a crowd tick with no overlap. `pnpm verify` green.
- T-3.07 — Behaviour tree runtime: `shared/src/ai/bt.ts` — sequence, selector (memory by default, `reactive: true` to re-check guards and pre-empt), parallel (`success: "all" | "one"`), inverter, cooldown and timeout in absolute ticks, condition and action leaves with a `running` status and an optional `halt` for abandoned actions. Trees are JSON in `data/trees/*.json`, validated by hand at import (`TREE_DEFS`; `idle` is the one committed tree); `buildTree` binds leaves by name to a server-filled `BtRegistry` and fails at load naming any unregistered one. Each instance has its own run state and a seeded `Sfc32`; no clock, no platform imports. `Blackboard<S>` (`ai/blackboard.ts`) is typed by its shape. Tests cover every node's semantics, re-entry, ticks-not-calls timing, load errors, and same-seed runs producing the same 200 actions. `pnpm verify` green.
- T-3.08 — Brains on the session: `server/src/ai/Brain.ts` wraps one behaviour tree instance per bot slot (blackboard `{ intent }`, the entity read live as ctx, seed from netId and the slot's brain generation). `Session` gives every bot slot a brain at construction and thinks each on `tick % 3 === netId % 3` — six brains, two a tick; a join stops the brain (halting its running action) before the next step, a leave builds a fresh one from the entity as it stands. With a `navMesh` in the new `SessionOptions`, a brain's latest intent is walked every tick by a lazily made `PathFollower` and steered by `Avoidance`; a bot whose brain has never wanted anything keeps its input untouched, so idle brains change nothing (`pnpm bot` output byte-identical before and after, in-process and over ws). Tests (`Brain.test.ts`): exact every-third-tick cadence per slot, all three phases two-per-tick, walking on non-think ticks, join/leave with netId, position and health surviving both swaps; cost logged (~0.12 µs an idle think; ~33 µs/tick session step with six idle brains, ~95 µs with six walking). `pnpm verify` green.
- Escape/F11 release: with Keyboard Lock active, one Escape press now frees the mouse (fullscreen stays) and F11 leaves page fullscreen and frees the mouse, instead of Escape needing a two-second hold and F11 doing nothing. Clicking the canvas still takes the mouse and fullscreen back. `LocalInput.releaseKey`; test in `LocalInput.test.ts`. `pnpm verify` green.
- T-3.09 — AI debug view: protocol 17's `AiDebugRequest`/`AiDebug` (tree path, intent, corridor, cones, targets, cover), sent at 10 Hz only to clients that asked and only by a host with `AI_DEBUG=1` (the in-page session allows it), drawn by a B-key overlay whose geometry is a pure function of the message
- T-3.10 — Enemy entities: protocol 18 adds an `Enemy` component (archetype index, faction) replicated beside `Transform`, `Velocity`, `Health` and `Crouch`. Archetypes are data (`enemies.json`, one `rifleman`, parsed by hand in `sim/enemies.ts`: health, weapon, tree, `downable: false`, corpse time, perception and accuracy blocks). `Session.spawnEnemy` gives each enemy a netId from its own bounded band (2000..16383; projectiles move up to 16384 so the band's two-byte varuints go to the many long-lived entities) and a brain of its own; enemies think at 10 Hz, walk their intent through path following and avoidance, step through `stepCharacter`, are recorded into the hitbox history every tick and are hurt by shots and blasts through `applyDamage`, whose new `downable` argument kills at zero. A dead enemy's brain stops and its input goes idle the moment it dies; the corpse despawns after its corpse time and leaves the hitbox history. Tests (`enemies.test.ts`, over the wire): spawn/move/despawn in decoded deltas; a head shot at 19.9 m through NetSim at 75 ms each way lands on the head zone of an enemy walking across the line of fire (it misses without the rewind); dies without going down, frozen and thoughtless from the kill, despawned within a tick of schedule; a frag kills one; 64 enemies, a projectile, slots and range targets never share a netId and ids are never reused. Shared tests cover the parser, the bands, non-downable damage and the component's delta round trip. `pnpm bot` output byte-identical. `pnpm verify` green.
- Alt+Tab in fullscreen: Keyboard Lock now locks an explicit key list (`LOCKED_KEY_CODES`, every `KeyboardEvent.code` but Tab) instead of calling `lock()` with no arguments, which captured Alt+Tab too. Tab reaches the OS, so Alt+Tab switches windows out of the fullscreen game; Ctrl+W, Alt+F4, Escape and F11 stay reclaimed. `keyboardLock.ts`; test in `keyboardLock.test.ts`. `pnpm verify` green.
- Alt+Tab in fullscreen, second pass: Alt and Meta are now out of `LOCKED_KEY_CODES` too. A locked key never reaches the OS, so with Alt locked the OS never saw Alt held and got Alt+Tab as a bare Tab; unlocking Tab alone did nothing. Alt+Tab, Win+Tab and Cmd+Tab now switch windows; Ctrl shortcuts (B-06) stay locked. `keyboardLock.ts`; test in `keyboardLock.test.ts`. `pnpm verify` green.
- T-3.11 — Enemies in the page: remote soldiers and enemies are drawn by one path, `client/src/character/remoteSoldiers.ts` (moved out of `main.ts`): same rig, pose driver, weapon layer, hit reactions and feet; an entity's `Enemy` component picks the new `enemy` palette (cool slate against the squad's warm olive, darker overall — asserted on atlas averages) and its archetype's weapon, and it has no slot, so no roster, HUD or revive prompt ever names it. A dead enemy lies in the death pose until it despawns; `NetClient` drops a remote from `remotes()` once the render clock passes its despawn and forgets it after, and the renderer removes and disposes everything it made for it. `?enemies` puts three riflemen (one standing, two patrolling on the range navmesh, respawned after despawn) in the in-page session. Fixed a pre-existing bug: choosing the in-page session more than 5 s after load dropped it for a heartbeat timeout (`LocalServer` now runs the session on its own clock). Browser run shows an enemy standing, walking, falling and despawning. `pnpm verify` green.
- T-3.12 — Interest management: each client is sent only the entities within ADR-012's 120 m of its own slot (`server/src/session/relevance.ts`), slots always, with a 10 m hysteresis; each client's delta baselines are now the views it was sent, so leaving is a despawn and re-entry a spawn with current state through the existing delta path (no protocol change). Tests over the wire: despawn for one client only, return with current position/health/Enemy, sparse acks through repeated leave/re-entry with no missed baseline. `pnpm bench:bandwidth` gains six slots + forty walking enemies on a real session: 8.5 KB/s per client, inside the 18 KB/s target (and its synthetic fixture, broken since Health grew to six fields, is fixed). `pnpm bot` output byte-identical. `pnpm verify` green.
- T-3.13 — Vision and awareness: `shared/src/ai/perception.ts`, pure functions of (observer, target, world) — a horizontal view cone tested against the table cosine of the half angle (no `atan2`), range, line of sight by `rayWorld` from the eye to the target's shin, chest and head up its stance height (the blast's probes; exposure is the clear fraction), and awareness 0..1 that rises by a product of data factors (distance, stance, motion, firing, place in the cone, exposure, capped) and decays out of view, detection at `detectAt`. The rifleman's perception block grew eleven validated numbers. Committed rifleman, 30 Hz: standing at 40 m detected in 0.70 s, crouched 1.17 s, prone 2.77 s; standing at 10 m 0.27 s, 75 m 3.3 s; point blank, sprinting and firing 0.23 s (never one think at 30 or 10 Hz). Not wired into brains yet (T-3.14).
- T-3.14 — Hearing, memory and target choice: `shared/src/ai/stimuli.ts` (shot at the shooter, impact and near miss at the point, detonation at the blast, sprint at the sprinter; loudness radius and confidence per kind in `data/stimuli.json`; hearing is a radius through walls) and `shared/src/ai/memory.ts` (per target a last known position, time, confidence decaying linearly to forgotten at `forgetSeconds`, visible/downed flags and a threat time; target choice scores visible × proximity × threat, downed scaled by `downedFactor` = 0 so a downed target is chosen only when nothing else is known; tuning in `data/memory.json`). `Session` emits the stimuli from `applyFire`, `detonate` and slot movement; every living enemy hears the squad's each tick and, on its think tick, steps T-3.13 awareness per living slot, remembers the detected, forgets the dead and picks `enemy.target`. No brain acts on it yet; `pnpm bot` output byte-identical. `pnpm verify` green.
- Host cost protection: an optional shared join key (`JOIN_KEY`, a Fly secret) is checked before any room is made or looked up — a missing or wrong one is the new typed refusal `bad key`, and the lobby gains a Key field remembered per browser and never put in the share link; `pnpm bot --key`. Players are dropped after `IDLE_TIMEOUT_MS` (default 10 min) with no movement, look, button or shot, and after `MAX_SESSION_MS` (default 4 h) regardless, as `idle` / `session limit`, so a held-open socket can no longer keep the auto-stopping machine up forever. Protocol 19 (Join carries the key; three new disconnect codes). Tests: `Connection.test.ts`, `SessionHost.test.ts`, `protocol.test.ts`, `config.test.ts`. `pnpm verify` green.
- T-3.15 — AI fire through the authoritative path: a brain sets `fireAt` and `Session.fireEnemies` shoots that soldier every tick through the human path (`traceShot`, split out of `applyFire`; the archetype's weapon, `tryFire` cadence, bloom, magazine and reload, `applyDamage`, the same `HitEvent`) with no rewind, only from a point on the target it can see. `server/src/ai/aim.ts`: aim error cone from the archetype (base × distance × target speed × suppression × an acquire factor that time on target settles), seeded per tick/netId/shot; the rifleman's accuracy block has nine validated fields incl. trigger discipline (`holdBloomDeg`) and the hit band it is tuned to. Committed rifleman hits 57.7 % of 300 shots at 20 m (band 40–75 %); 3.86° mean error on acquiring vs 1.52° settled. `pnpm bot` byte-identical; `pnpm verify` green.
- T-3.16 — Suppression in the sim: `shared/src/sim/suppression.ts` + `data/suppression.json` — a near miss is a round's path within 1.0 m of a soldier's capsule surface without touching it (segment against the capsule's axis); near misses, impacts within 2 m and blasts raise a 0..1 level that holds 0.75 s then falls 0.35/s. Every slot and enemy carries one; only the other side's fire counts. An AI's aim widens through its archetype's `suppressionFactor`; a human's weapon cone widens by 1.5° at full (`currentConeUnits`/`tryFire` take the extra), from the level as replicated by the new `Suppression` component (id 10, 6 bits; protocol 20), and the page's predicted cone and crosshair read the same level. The session's near-miss stimulus uses the capsule test too; `stimuli.json`'s `nearMissM` is gone. Measured spread +1.46° at full suppression (data 1.5°). `pnpm verify` green.
- T-3.17 — Suppression in the page: `ui/suppressionLook.ts` draws a vignette and drains the colour (a `backdrop-filter` layer under the HUD) as a pure function of the replicated level — nothing at 0, monotonic, identical at 30 and 120 fps; `cameraShake.suppressionJolt` jolts the camera once per rise in the level (one near miss's worth per `SUPPRESSION.nearMiss`); the crosshair's gap (`ui/crosshair.ts`, moved out of `main.ts`) is the replicated spread. `?suppress` puts a rifleman firing past the player's camera in the in-page session; in Chromium the vignette peaked at 0.75, saturation 0.4 and the crosshair gap opened 26 → 42 px. `pnpm verify` green.
- T-3.18 — Cover points in the bake: `tools/src/nav/cover.ts` stands a soldier 1 m apart along every box face wide enough to hide a body, keeps the points on the mesh with the footprint clear of every box, and classes each **low** (box top over the crouched eye, 0.95 m) or **high** (at or over the standing eye, 1.55 m) with the face's outward normal. `pnpm gen:nav` writes them into `baked/<world>.ts` beside the mesh (`BakedNav.cover`, `bakedCoverFor`) under the same staleness hash, which now covers the spacing and eyes; mesh bytes unchanged. Range: 70 points, 32 low, 38 high. `pnpm verify` green.
- T-3.19 — Cover query and reservation: `server/src/ai/cover.ts` (`CoverSystem`, tuning in `cover.json`) scores baked cover points within 30 m of path by protection (no threat eye sees the concealed body's shin, chest or eye), a firing position (stand over low cover, side-step 1.2 m out of high; required for combat), path cost and crowding friends, and reserves the choice until its holder leaves it after arriving, dies or asks again; `stillProtects` says when a moved threat has flanked it. Line of sight is shared per threat eye and paths are found branch-and-bound (exact): 40 brains 6.4 ms warm on the range, down from 122 ms. `pnpm verify` green.
- T-3.20 — The rifleman's fight: `data/trees/rifleman.json` over leaves in `server/src/ai/actions/` — take cover facing the threat, peek and fire a burst (stand over low cover, side-step out of high), reload in cover, relocate when flanked, advance when unopposed, pushed down by suppression and damage. The session takes baked cover via `SessionOptions.cover` and turns a brain's crouch, reload and facing into its input (a finished reload now settles before a standing request can restart it); an enemy fires from its own eye in its stance. T-3.19's probes now include the body's edges. `pnpm sim-run --scenario cover-duel`, 20 seeds: cover within 5 s in 100 % of runs, worst 13.1 % exposed while not firing, never reloads exposed in cover, relocates within 1.13 s of a flank. The archetype's tree stays `idle` until T-3.23. `pnpm verify` green.
- Grenade reach and projectile tuning: a frag thrown at its best angle landed 14.6 m out at most (level: 9.2 m) — a soldier throws 30-40. `projectiles.json`'s frag is now 20 m/s, 10° loft, 9.81 m/s² gravity, 0.1/s drag and a 3.5 s fuse (so a 30-45° throw lands before it goes off), with less bounce and more roll drag so it settles rather than skates: level lands 18.9 m, best angle 35.6 m, and one dropped at your feet still goes off within 2 m. New **Projectile tuning** panel (`ui/ProjectilePanel.ts`): sliders for every frag and rocket number bar the drawn radius, a reach readout (where a level and a 45° throw land and go off, from the server's own stepper), and a paste-back block in `projectiles.json` shape. Edits apply to `ThrowQA`'s working rows and are handed to the in-page session (`Session.tuneProjectile`, per session; the shipped table is untouched), so the server throws what the arc shows; a remote host keeps its data. Tests: `ui/ProjectilePanel.test.ts` (paste-back complete; reach floors; lands before its fuse to 45°; dropped at your feet; tuning reaches only its session); projectile tests now wait on the data's fuse, not a fixed 85 ticks. `pnpm verify` green.
- T-3.21 — Suppress and flank: enemies spawned into one group (`EnemySpawn.group`, `server/src/ai/group.ts`, `group.json`) share a target; once it has been out of every member's sight for 1 s the group hands out a suppressor — firing just over its cover at its last known position without a line of sight (`suppressAt`, T-3.15's exception) — and a flanker, sent to a reserved cover point that sees the target's low body along the cheapest route with the target's sight priced 8× (`NavMesh.pathAvoiding`: a Detour filter over penalised polygons, restored after; on the range's coarse solo mesh the group also tries hidden stopovers). Tree: role branches first. `pnpm sim-run --scenario pinned`, 20 seeds: suppression ≥ 0.3 for ≥ 68 % of every flank, flank reached in 100 % of runs, the walk seen 61 % against the direct route's 77 %. `pnpm verify` green.
- T-3.22 — AI grenades: a rifleman whose target has sat still in cover for 3 s, 8–35 m off, searches launch pitch through `projectileArc` from the session's own launch (`server/src/ai/throw.ts`, `throw.json`) for an arc that goes off within half a blast radius of it and clear of itself and every friend, then throws it through the human throw path from its own pouch on the row's cooldown (`grenadeTarget`/`throwGrenade`, first in the rifleman tree).
- T-3.23 — Rifleman and MG: five archetype shapes in the schema (the MG's `deploy`, RPG's `launcher`, sniper's `scope`, officer's `command`), two in the data; the MG carries an enemy-only LMG, fires only once still 1.5 s (`Session.deployed`), in 45-round bursts (every enemy now bursts by data), is its group's suppressor first and relocates only after 3 s flanked and 20 s since the last, at a walk. `pnpm sim-run --scenario mg`: 1.70× the rifleman's suppression per second, 0.31× its relocations, no round undeployed.
- T-3.25 — Formation following: two fixed fireteams in `data/squad.json`; a friendly bot (`friendly` tree, `follow` leaf, `server/src/ai/friendly/formation.ts`) follows its fireteam's lead — first human in the fireteam, else in the squad, else slot 0 — at a wedge or file place laid along the lead's own trail, at the lead's pace, closing up when it stops. Over a 76 m route five bots stay in band 92–100 % of the way and rejoin within 1.2 s of any corner. Opt-in via `SessionOptions.brainTree`.
- T-3.26 — Friendly bots fight and revive: a bot slot is a `CombatBody` running the rifleman's leaves with its own perception (`squad.json` bot.archetype), fire through one `aiShoot` shared with enemies that holds the trigger while a squadmate's capsule is on the line, cover only within its formation band, and revives through the human's held-interact path (the `isBot` exclusion lifted; same range and timer). `pnpm sim-run --scenario squad`: 30/30 riflemen killed over 10 seeds, 0 friendly hits, 17 bot revives.
- T-3.27 — Orders on the wire (protocol 21): `Order` (move/attack/hold/regroup/revive, to a slot, a fireteam or all, with a point or target) and `Mark` from a client, `Orders` and `Marks` broadcast whole by the host, under tag 15's new three-bit sub-kind beside AI debug. The session takes an order only from a seated human, well-formed (`sim/orders.ts`), at a target that exists, and only onto bots; the last order stands; marks stand `data/orders.json`'s 20 s, three a player.
- T-3.28 — Bots carry out orders (`server/src/ai/friendly/orders.ts`, first in the friendly tree): move to cover at the point and hold there, attack to a firing position and kill, hold under fire, regroup into the band, revive the named squadmate; each reported done, failed (an unreachable move at once) or replaced (`Session.orderReports`). A marked enemy outranks a closer unmarked one for every bot, and an attack order's target outranks both.
- T-3.29 — Order wheel and marking in the page: hold Q for a five-sector wheel (move, attack, hold, regroup, revive, clockwise from the top) that takes the mouse while open, number keys to address a slot (1–6), a fireteam (7–8) or everyone (0), release latched like the throw's to order at the converged aim point and the soldier under it; F marks the enemy under the crosshair, else the point; orders and marks drawn in the world from the host's `Orders`/`Marks` broadcast only (`ui/OrderWheel.ts`, `ui/OrderMarkers.ts`); the crosshair tints over an enemy or a downed squadmate; `?squad` gives the in-page bots the friendly tree and cover. Each order issued to a bot in a browser run.
- T-3.31 — The grey-box mission map: a second named world, `greybox-01` (`data/worlds/greybox-01.json`, baked and committed), with an optional `mission` block in the world format (`loadMission`: start, objective, routes by role, enemy spawn zones, and the file's own checks). A 4 m spine splits an open overwatch lane (west, long sight lines, low walls) from an assault lane zigzagged by alternating high walls with crates between (east), both to side doors of an objective compound; the squad starts on the usual spawn line. Tests: both routes connected and 94–95% on polygons the other never touches (floor 70%), in their own lanes, every spawn zone on the mesh, cover never more than 17 m (overwatch, cap 24) and 4 m (assault, cap 8) away, a 75 m sight line on the overwatch route and none over 34 m on the assault, and a soldier walks each in the controller with no stuck repaths. `?world=` for the in-page session.
- T-3.32 — Encounters and spawning: an encounter file per world (`data/encounters/greybox-01.json`, `sim/encounters.ts`, validated against its world's mission): groups of archetypes in a spawn zone with a posture (hold, patrol, garrison) and a trigger (start, time, the squad entering an area, another group dead), with reinforcement waves. `server/src/ai/director/spawner.ts` plays it on a session (`SessionOptions.encounter`): candidate points nearest a zone's centre first, any a human's eye can see at a probe height skipped for the next, none on an occupied point, and never more alive than the cap — what it holds back waits. Enemy trees end in `atEase` (`actions/posture.ts`): their posture, or standing down with none; a garrison fights from inside its area. Tests: each trigger fires on its tick and not before, waves keep a group alive until the last has died, a visible candidate is passed over for the first hidden one, every-candidate-visible waits, the cap holds under six waves of four, and on the grey-box map the garrison takes cover inside the objective, the patrol walks out and back, and the hold stays on its post facing the start.
- T-3.33 — The director (`server/src/ai/director/director.ts`, `data/director.json`): intensity 0..1 from squad damage over a sliding window, enemies in contact and squad suppression, weighted and capped; a group's next wave is held while intensity is above `holdAbove` and brought forward to its `minSeconds` while below `forwardBelow`, never outside the encounter's new `waves.minSeconds`/`maxSeconds`. Every wave's member counts and the alive cap scale by a budget row per HUMAN seated (ADR-001), fixed when the wave is sent. The spawner takes a `Pacing` (the director, or `FIXED_PACING`). Tests: intensity per part and its window, bad tuning refused, a hot fight holds a wave to exactly its 40 s maximum and a calm one sends it at its 10 s minimum, a held wave goes the tick intensity drops, a human joining mid-mission sizes the next wave and not the queued one, and on a real session one human and five bots get the one-human garrison (3 of 4, cap 5) and six humans the file's (4, cap 10).
- T-3.34 — The objective: clear the world's objective area and hold it for `data/mission.json`'s 30 s (no living enemy inside, a living squad soldier inside; an enemy inside resets it, stepping out pauses it), evaluated on the server (`session/mission.ts`), failed on a squad wipe, with no respawn during a mission (`respawn: false`) so a wipe can happen. Protocol 22: `Mission` (state, clear, held and hold ticks, attempt) host to client on every change, each second of a hold and on seating, and `MissionRestart` client to host, under one new extended sub-kind with a two-bit variant. A restart (honoured once the mission is over) clears every enemy, projectile, order and mark, respawns the squad on the start line and plays the encounter again from its first tick. The HUD line reads the message (`ui/missionHud.ts`); P asks for the restart; `?mission` plays it in the page.
- T-3.35 — The mission, headless: `pnpm sim-run --scenario mission --seeds 20` (`tools/src/scenarios/mission.ts`, `mission.json`) plays greybox-01 with six friendly bots against its encounter at the one- and six-human budgets (`SessionOptions.testHumanCount`, this tool's alone), a scripted leader ordering each fireteam up its route (`Session.orderFrom`) and bots sent to revive. Baseline over 20 seeds: completion 30% (1 human) and 15% (6), enemies under fire in cover 32%, 7.4 squad suppression episodes per engagement; floors set just under it (completion asserted over 10+ seeds only), and the imbalance filed as B-11. AI cost at 40 riflemen and 5 bots measured inside the session (`profileAi`): 10.7 ms a tick at first, 4.2 ms (12.5% of the tick, ceiling 25%) after `rayWorld` rejects boxes outside the segment's bounds and unrolls its axes (identical answers on 16,000 seeded rays) and a flank assignment marks the target's sight once for every route it prices (`NavMesh.avoiding`). `bench:tickrate` prints it; a three-seed CI job runs the scenario.
- QA on the deployed site (T-3.35 follow-up): the host can run the AI (`HOST_AI=1`, on in `fly.toml` with `AI_DEBUG`): rooms get their world's navmesh (one per world, shared) and cover, the friendly tree on bot slots, and on the mission map the encounter and mission. The lobby's **Map** picks the world a new room is built on, carried in the Join (protocol 23, `Join.world`, ignored for an existing room and for a world the host lacks). Tested over loopback (`hostAi.test.ts`) and end to end: the production build hosting a Mission room on a `HOST_AI=1` host drew the mission HUD with five bots following. `docs/playtests/qa-backlog.md` rewritten for the published site: solo links with their flags, two-person runs through the lobby's Map, CI's mission job in place of every terminal command.
- E-2.7 broken out (planning, no code): ADR-017 records the owner's decision that combat audio is made in-house by AI — effects synthesised from code and rendered offline into committed files, voices recorded by people and processed by code (pitch and formant shift, voice profiles, radio and shout treatment, variants), placed with Web Audio, judged by the owner's ear on a sound board on the deployed site. PLAN.md §7.10 adds T-2.44–T-2.50 (synthesiser and render pipeline, positional playback and `?sounds`, weapon sounds, world and body sounds, the voice pipeline, callouts in play, 🧍 sign-off); §9 Q2 is answered for audio; `docs/audio/voice-script.md` is what to record and how to upload it with nothing local. ADR index gains 016 and 017.
- M4 broken out (planning, no code): PLAN.md §7.11 adds T-4.01–T-4.34 across E-4.1–E-4.9 — asset pipeline and CI budgets, runtime loading and LOD, level format v1 with a ~25-piece kit and mission-01, multi-objective mission scripting with checkpoints, reconnect and ready-up, persistence, HUD/menus/Team Leader and Marksman, the mounted MG, multi-region deployment, and the 🧍 exit gate. Rules: collision stays axis-aligned boxes (90° placement), budgets fail CI from the first asset, §4.1's cut holds, everything lands testable on the deployed site. Two owner decisions are tasks: art sourcing (🧍 T-4.01, ADR-018) and where progress lives (🧍 T-4.21, ADR-019, §9 Q5). TASKS.md gains the M4 tables.
- T-4.01 (options only; the owner decides): `docs/adr/018-art-sourcing.md`, Proposed, compares code-authored art, Blender by hand, purchased packs, commissioned art and two mixes (code world with bought mocap; code world with bought characters and mocap) on cost, quality ceiling, iteration speed, ADR-013's budgets and the ~35 clips, and notes that about twenty of those motions already exist as procedural poses. ADR index gains 018.
- T-4.02 — The asset pipeline: `pnpm gen:assets` turns every source glTF in `assets/src/` into a web copy in `client/public/assets/` (dedup, prune, KTX2 UASTC with Zstandard and mipmaps, meshopt geometry) and an entry in `shared/src/data/assets/manifest.json` (id, input hash, hash, bytes, triangles, bones, materials, texture sizes, LOD levels, collision boxes from the scene's extras). All committed with a staleness hash like `gen:nav`. The test asset is the code-built soldier (`pnpm export:soldier`): 188,588 → 79,828 bytes, back within 0.104 mm a vertex on the same bones. Dev dependencies only: gltf-transform, meshoptimizer, ktx2-encoder, ktx-parse, pngjs, three (tools).
- T-4.03 — Asset budgets in CI: `shared/src/data/assets/budgets.json` (ADR-013's ceilings by class: character 15k triangles, 65 bones; weapon, prop and kit rows proposed; textures KTX2, power of two, capped per class; 80 MB download). `checkBudgets` (shared) names the asset, the number and its limit for every overrun; it runs as a test in `pnpm verify` and as `pnpm check:assets` in CI. Each source declares its class on its scene's extras (`sandline.class`), which the pipeline requires and the manifest carries.
