/** Every generated piece (T-4.04). `pnpm gen:art` writes each to `assets/src/<id>.glb`. */
import type { Piece } from '../piece.ts';
import { WALL_PLASTER_4M } from './wall-plaster-4m.ts';

export const PIECES: readonly Piece[] = [WALL_PLASTER_4M];
